import { createSlice } from '@reduxjs/toolkit';

// export interface InitialState {
// }

// export interface IState {
//   addAdv: InitialState;
// }

export const addAdvantSlice = createSlice({
  name: 'addAdv',
  initialState: {},
  reducers: {},
});